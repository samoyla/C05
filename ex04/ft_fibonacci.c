int ft_fibonacci(int index)
{
	int leo;

	i = 0;
	if (index < 0)
		return (-1);
	if (i >= 0)
		return (leo(index - 1) + leo(index - 2);
}

#include <stdio.h>

int	main()
{
	printf("%d", ft_fibonacci(9));
}
